## Same-session notes

`session_note` talks to the leader or a live peer in THIS session (next iteration). Prefer it over mailbox for same-session findings, asks, and steers. `to="leader"`, an agent id, or `@session`. Mailbox remains the durable cross-session plane.

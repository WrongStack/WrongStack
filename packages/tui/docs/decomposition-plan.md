# TUI God-File Decomposition Plan (v1.1 — approved)

Targets: `src/app.tsx` (1059 L), `src/run-tui.ts` (652 L), `src/app-key-handler.ts` (1102 L),
`src/hooks/use-tui-slash-commands.ts` (934 L). Line counts as of 2026-09-10; re-read bodies at each
phase start (older notes cite pre-decomposition line numbers that no longer exist).

## Review decisions (2026-09-10)

- **D1 — Phase order:** approved as drafted: Phase 0 → 1 (`run-tui.ts`) → 2 (`use-tui-slash-commands.ts`) → 3 (`app-key-handler.ts`) → 4 (`app.tsx`). Least React-coupled first; App last because it consumes the four stabilized seams.
- **D2 — Facade names/groupings:** names approved as drafted. Membership is finalized by the phase's read-through (A0 for App); membership deltas are recorded in that step's commit message. Name changes require a new review.
- **D3 — Phase 0 characterization tests:** all three included (public-API surface freeze, key-handler replay corpus, slash-registry enumeration). Test-only commits; each must pass against current code before any move.
- **D4 — Plan document:** this file (`packages/tui/docs/decomposition-plan.md`), docs-only commit.

## Behavior-preservation contract (every step)

1. No exported-signature changes. `runTui(opts)`, `createAppKeyHandler(options)`, `useTuiSlashCommands(props)`, `App(props)` and all current exports keep exact types. Old modules become re-export shims during transition (existing repo convention, cf. `app-reducer.ts` re-exporting `reducers/helpers.ts`).
2. Pure code motion. Bodies move verbatim; only import paths and wiring change. Bugs discovered mid-move are logged, not fixed — fixed in their own commit after the move lands green.
3. Hook-order stability. In `App`, hooks move into facade hooks only if every facade is called unconditionally in a fixed order at the top of the component.
4. Ref identity. The ~20 `MutableRefObject`s threaded through `AppKeyHandlerOptions` are created once and passed down; never re-created inside extracted modules.
5. One step = one commit = green gate. Gate: `pnpm --filter @wrongstack/tui typecheck` + `pnpm --filter @wrongstack/tui test` (baseline: 342 files / 5682 tests / ~128s, recorded 2026-09-10).
6. Re-read before each phase; update this file's execution log as steps land.

## Phase 0 — safety net

| Step | Deliverable | Gate |
|---|---|---|
| 0.1 | Baseline recorded: typecheck + full suite green | logs in `.temp_files/tui-baseline-*.log` |
| 0.2 | `tests/public-api-surface.test.ts` — runtime export freeze for `index.ts`, `app.tsx`, `app-reducer.ts`, `run-tui.ts` | vitest run |
| 0.3 | Key-handler replay-corpus test: (input, key, state) tuples → dispatched actions through current `createAppKeyHandler`, snapshotted | vitest run |
| 0.4 | Slash-registry enumeration test: names + order of commands registered by `useTuiSlashCommands` | vitest run |
| 0.5 | Journey tests confirmed green (`app.comprehensive`, resume/render timelines) | part of 0.1 |

## Phase 1 — `run-tui.ts` → ≤120-line orchestrator

| Facade | Absorbs |
|---|---|
| `resolveTuiLaunchPlan(opts)` | TTY guard, capability probe, mouse opt-in, silence decision |
| `setupTuiSession(opts)` | session/client/agent bootstrap (wraps `createRunTuiClientRegistration`) |
| `mountInkApp(deps)` | `render()` + `inkStdin` wiring + raw-Ctrl+C arming + resize erase handler; returns `{ instance, detachResize }` |
| `createExitOrchestrator(...)` | six exit paths through idempotent `cleanup()`/`settle()`; extends `run-tui-teardown.ts`, no duplication |

Invariants: raw-Ctrl+C listener attaches only after Ink owns stdin; cleanup stays idempotent across all six paths; `run-tui.comprehensive/guard/teardown` tests unchanged and green.

## Phase 2 — `hooks/use-tui-slash-commands.ts` → 5 slice hooks

Parent keeps its exact 22-field signature and calls slices in today's registration order (pinned by 0.4):
`useDesignKitSlashCommands`, `useResourceSlashCommands`, `useSettingsSlashCommands`,
`useSessionSlashCommands`, `useProviderSlashCommands`.

## Phase 3 — `app-key-handler.ts` → ordered route chain

Orchestrator keeps the exact `createAppKeyHandler(options)` signature and documents precedence:
1. `createBusyKeyRoute` → 2. `createOverlayKeyRoute` → 3. `createPointerRoute` (explicit `overlayOpen` flag) → 4. `createPickerRoute` → 5. `createComposerRoute`; shared `createPastePipeline`.
Each route takes only the options fields it uses. Acceptance: 0.3 replay corpus produces an identical dispatch trace.

## Phase 4 — `app.tsx` → composition shell (≤ ~250 lines)

A0 first: read the full body; produce a hook → produced-values → consumers table; finalize facade membership (names stay).
Facades (approved names): `useAppState`, `useAppRefSpine`, `useAppBridges`, `useAppEnvironment`, `useAppSession`, `useAppExecution`, `useAppComposer`, `useAppPanels`.
Sub-phase order A1→A8: state+ref spine → bridges → environment → session → execution → composer → panels → AppView wiring + dead-local cleanup.

## Out of scope (separate workstreams)

AppProps slicing (H2), `/clear` leader-abort gate (H5), swallowed key-handler errors (H6), Scrollbar memoization (H7), reducer module-graph purity (M2). Moves and behavior fixes are never mixed in one commit.

## Execution log

- 2026-09-10: plan v1.1 committed (D1–D4 recorded).
- 2026-09-10 Phase 0.1: suite baseline GREEN — 342 files / 5682 tests / 151.31s. Typecheck baseline RED on committed code (`tests/sidebar-pin-and-scrollbar.test.ts:30,37` — `settingsValueChange` actions missing required `delta`; pre-existing, unrelated to decomposition). Repaired with behavior-neutral `delta: 1` (field 61 is a pure toggle at `reducers/settings-values.ts:618`; delta ignored). Full typecheck GREEN after repair.
- 2026-09-10 Phase 0.2: `tests/public-api-surface.test.ts` authored; targeted run GREEN (4 tests, 13s). Committed `3e7978a4c`.
- 2026-09-10 Phase 0.4: `tests/slash-registration-enumeration.test.ts` authored; targeted run GREEN (2 tests, 4.5s). Committed `fa3fd134a`. Registration inventory: 23 commands / 14 effects, order pinned solo→resume.
- 2026-09-10 commit-path note: snapshot-sync hook fails closed on the shared worktree's peer edits; per review decision, scoped commits use `git commit --no-verify --only -F <msgfile> -- <paths>`. Caveat discovered: `a7fc267fa` (repair) absorbed 3 already-staged generated artifacts (`architecture/hotspots.json`, `docs/reports/architecture-health-current.{json,md}`) because the index was shared; left as-is (regenerated artifacts, next peer run refreshes them) — all later commits fenced with `--only`.
- 2026-09-10 Phase 0.3: `tests/key-handler-replay-corpus.test.ts` authored (20 cases, snapshot-per-case); targeted run GREEN (20/20) with setDraft-enriched traces; snapshots regenerated via `vitest -u` (4 composer-route snapshots updated). Committed `5f1f82b3e`. Phase 0 complete.
- 2026-09-10 Phase 1 R1: `resolveTuiLaunchPlan` extracted verbatim to `src/run-tui-launch.ts` (TTY guard, capability probe, mouse opt-in, `silenceTerminal()` call; lifecycle init now follows the plan resolution — independent probes, single caller unchanged). Gates GREEN: typecheck (both projects) + targeted run-tui suite 64 tests (comprehensive 31, teardown 7, guard 1, silence-terminal 19) + enumeration 2 + api-surface 4. Includes test-side stub fixes for the 0.4 enumeration test (full SkillLoader members + boundary cast). Committed alongside this log update.
- 2026-09-10 Phase 1 R2: `setupTuiSession` extracted verbatim to `src/run-tui-session.ts` (wraps `createRunTuiClientRegistration`; takes `isCleaned` accessor — the `cleaned` flag stays local to runTui because every cleanup path reads it). Gates GREEN: typecheck (both projects) + targeted run-tui suite 64 tests + enumeration 2 + api-surface 4. Committed alongside this log update.
- 2026-09-10 Phase 1 R3: `mountInkApp` extracted verbatim to `src/run-tui-mount.ts` (render with `exitOnCtrlC:false`/`maxFps:10`, raw-Ctrl+C stdin arming after Ink owns stdin, resize `\x1b[J` erase handler; internal catch reports via `onStartupFailure` and returns null — runTui keeps its outer catch as safety net and wires waitUntilExit via the returned hoisted `mount` handle; the `/clear` rationale comment now references Ink's Instance API; chimera review of the diff: 0 findings). Gates GREEN: typecheck (both projects) + targeted run-tui suite 64 tests + enumeration 2 + api-surface 4. Committed alongside this log update.
- 2026-09-10 Phase 1 R4: `createExitOrchestrator` extracted verbatim to `src/run-tui-exits.ts` — single owner of the six exit paths (natural settle, SIGTERM/SIGHUP/SIGBREAK, SIGINT rapid-press, raw-stdin Ctrl+C watcher, process exit listener, requestExit) through idempotent cleanup, composing setupTuiSession and createDurableTeardown; owns the `cleaned`/alternate-screen flags, registration handle, ink-instance ref, and the deferred resolve (`attachResolve`). runTui wires `exits.*` (settle/onRawCtrlC/getRunExitCode/recordExitCode/setInkInstance/markAlternateScreenActive) and keeps the title controller, trace tee, element construction, and waitUntilExit. Chimera review of the live tree: 0 findings (R4 file verified with all 8 accessors used). Phase gate GREEN: full typecheck + full tui suite (exit 0). Committed alongside this log update. Phase 1 complete.
- 2026-09-10 Phase 2 slice 1 (session): /solo, /mailbox, /autonomy, /resume moved verbatim to `src/hooks/use-session-slash-commands.ts` — part-parameterized ('head' | 'mid' | 'tail') so the parent interleaves domains while preserving the pinned 23-command registration order (positions 1, 10–11, 23; enumeration test green and untouched). Parent exports `TuiSlashCommandOptions` for the slices and builds a single `sliceDeps` bag; `RESUME_PICKER_SESSIONS = 500` moved with /resume; verbatim effect comments carried over. Membership deltas recorded per D2: /f rides with the provider slice and /theme with the settings 'appearance' part (positions 3 and 22 have no session-domain neighbor). Gates: typecheck (both projects) GREEN; enumeration 2/2; api-surface 4/4; full suite GREEN (exit 0) after rebuilding `@wrongstack/tools` dist — a peer build had wiped its chunks mid-run (stale chunk-VSOVZ5JI reference), environmental and unrelated to the slice.
- 2026-09-10 Phase 2 slice 2 (provider): /model and /f moved verbatim to `src/hooks/use-provider-slash-commands.ts` — single call at pinned positions 2–3 (no part param needed: both contiguous). Membership delta per D2: /f rides with provider (no dedicated slice among the five approved names). Chimera review: 0 findings (verbatim move verified on-disk, wiring closed by the TuiSlashCommandOptions annotation, no runtime import cycle, hook position unchanged). Gates GREEN: biome clean, typecheck (both projects), enumeration 2/2, api-surface 4/4, full suite exit 0.
- 2026-09-10 Phase 2 slice 3 (design-kit): /design (the longest single effect — full subcommand tree: picker, off, foundations, set, tune, swap, materialize, verify, kit pin) moved verbatim to `src/hooks/use-design-kit-slash-commands.ts` — single call at pinned position 4; the parent's now-dead `@wrongstack/core/design` import block moved with it. Chimera review: 0 findings (verbatim body, sliceDeps passes all 22 fields, slice destructures only slashRegistry/projectRoot/agent/dispatch, behavior identical). Gates GREEN: biome clean, typecheck (both projects), enumeration 2/2, api-surface 4/4, full suite exit 0.
- 2026-09-10 Phase 2 slice 4 (settings): /settings, /settings-get, /statusline (incl. inline on|off toggles), /lite + /full (paired registration with combined teardown), and /theme moved verbatim to `src/hooks/use-settings-slash-commands.ts` — part-parameterized ('core' at positions 5–9, 'appearance' at 22). Parent trims the settings-picker/STATUSLINE_ITEMS/hasPanelRoutedToSidebar/THEME_OPTIONS imports and the unused `Settings` type. One self-caught deviation during the move: the /statusline usage message was miscopied as a single-quoted string (killing `${item}` interpolation); biome's noTemplateCurlyInString flagged it and the original template literal was restored before landing. Chimera's final-state review: decomposition clean end-to-end, 1 defensive MEDIUM (part-param early-returns; suggested two-hook split = D2 name change → deferred to the Phase 2 wrap-up review). Gates GREEN: biome clean, typecheck (both projects), enumeration 2/2, api-surface 4/4, full suite exit 0.
- 2026-09-10 Phase 2 slice 5 (resource): /skill, the seven operational resource-menu browsers (fallback, tier, profile, provider-status, memory, worktree, git), and cron/prompts moved verbatim to `src/hooks/use-resource-slash-commands.ts` — single call at pinned positions 12–21. The parent hook is now a pure composer: five slice calls in pinned order, type-only imports, zero inline effects (934 → ~140 lines). Chimera review: 0 findings (effects preserved verbatim, same dep arrays and lifecycle wiring). Gates GREEN: biome clean, typecheck (both projects), enumeration 2/2, api-surface 4/4, full suite exit 0. Phase 2 complete.
- 2026-09-10 Phase 3 route 1 (paste): bracketed-paste accumulation moved verbatim to `src/key-routes/key-route-paste.ts` as `routePastePipeline(ctx, input): Promise<boolean>` — early-return restructure of the original `if (input) { ... }` block, behavior-identical (accumulation, error entry, inline commit on complete, detached timer-path commit). New `src/key-handler-context.ts` exports `KeyRouteContext` (= full `AppKeyHandlerOptions` + the factory-local `stdout`/`historyWidth`/`detach`), built once per session via `{ ...options, stdout, historyWidth, detach }`; `AppKeyHandlerOptions` is now exported; `pasteFlushTimerRef` left the parent destructuring (its only user moved) while `pasteAccumRef` stays (the mid-paste Enter guard). Two self-caught fixes: a wrong relative import path (`./` → `../`) and a `Promise.resolve` no-op wrap normalized to match HEAD byte-for-byte. Chimera review: all-clear. **Acceptance oracle: key-handler replay corpus 20/20 — identical dispatch traces.** Gates GREEN: biome clean, typecheck (both projects), api-surface 4/4, full suite exit 0.
- 2026-09-10 Phase 3 route 2 (busy-interrupt): the Ctrl+C unconditional escape hatch and the busy-state `routeBusyInterruptKey` delegation moved verbatim to `src/key-routes/key-route-busy.ts` as `routeCtrlCEscalation(ctx, input, key)` and `routeBusyInterrupt(ctx, key)` — called at their pinned positions (Ctrl+C before every modal/status guard; busy router after the panel-close routes). The seven busy-only option fields (runInterruptLadder, dismissedEscAtRef, streamingTextRef, confirmExitRef, activeCtrlRef, clearPendingConfirms, liveDirector) left the parent destructuring — they remain on `ctx` via the `AppKeyHandlerOptions` extension (chimera-verified). Chimera review: 0 findings ("no regression"). Gates GREEN: biome clean, typecheck (both projects), **replay corpus 20/20 identical traces**, api-surface 4/4, full suite exit 0.
- 2026-09-10 environment note: node_modules was repeatedly re-gutted by peer agents' concurrent `pnpm install` waves (os error 3/32 on different files each wave; "Already up to date" metadata desync). Countermeasures recorded in SAGE; recovery driver chained install+test in one process from packages/tui cwd.
- 2026-09-10 Phase 3 route 3 (overlay/panel): eight blocks moved verbatim to `src/key-routes/key-route-overlay.ts` (modal overlay — including the second leader session's concurrently-landed inspect-overlay host fields, captured verbatim after coordination mail — double-Esc with its disarm write, esc-panels + bash-mode exit, Ctrl+F/G/T + F2–F4 panel chords, F-key panel dispatch, Ctrl+B/Y toggles, SDD drill-down, settings-overlay + panel-escape router) — orchestrator calls them at pinned positions; `ESC_DOUBLE_PRESS_MS` moved with double-Esc; parent trims now-dead imports and the moved-only destructured names (they remain on `ctx` via the `AppKeyHandlerOptions` extension; hit-test imports intentionally kept for the still-inline pointer route). One test adaptation: kanban-panel-render's Ctrl+Y source-text pin repointed from `app-key-handler.ts` to the overlay route module — a location pin, not behavior; both assertions still hold verbatim. Chimera review: 0 findings (all 8 fns call original logic verbatim, ctx.* reachability confirmed, `lastEscAtRef` disarm points preserved, pinned call order matches). Gates GREEN: biome clean, typecheck (both projects), **replay corpus 20/20 identical traces**, api-surface 4/4, full suite 345/345.
- 2026-09-10 Phase 3 route 4 (pointer): the full mouse block moved verbatim to `src/key-routes/key-route-pointer.ts` as async `routePointerEvents(ctx, input, key)` (right-press copy fallback, wheel sidebar/history split, sidebar rail scrub, history scrollbar click/drag with inspect/copy/tool-control icons, drag-to-select, release-commits-copy, mouseMode-gated status-bar chips with `await openModelPicker()`, PageUp/PageDown, Ctrl+U/D paging — the enclosing `if (!overlayOpen)` gate carried into the route), plus the keyboard sidebar-focus scroll tail as `routeSidebarFocusScroll` (Shift+Tab toggle, ↑/↓, Esc/typing unfocus with its input fall-through). Parent trims 7 dead imports + 11 moved-only destructured names (hit-test/scrollbar/workspace-panels/sentinel imports now live only in the route module). Coordinated with the second leader session's concurrently-landed inspect-overlay additions (captured verbatim in route 3; this route's inspect icon branches read them via `historyScrollRef`). One mid-landing correction caught by self-review before gating: an over-trim briefly removed the hit-test imports while the pointer route was still inline — restored in the same pass. Chimera review: 0 findings ×2 ("removed ~370 lines exist verbatim", enclosing gate + mouseMode gate + await preserved, call order unchanged; its residual TS6133 concern closed by the post-trim green typecheck). Gates GREEN: biome clean, typecheck (both projects), **replay corpus 20/20 identical traces**, drag-selection-copy 36/36 (routing-level pin drives the real createAppKeyHandler), api-surface 4/4, full suite exit 0.

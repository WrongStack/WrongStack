{
  "name": "monorepo-pnpm",
  "version": "0.0.1",
  "private": true,
  "description": "Test fixture: pnpm monorepo with two packages",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}

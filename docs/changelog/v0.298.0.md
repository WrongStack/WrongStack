# v0.298.0 — 2026-08-01

## Highlights

- **Kanban architecture program** — Phase 0-4 complete: canonical task classifier, board kinds & retention filtering, shared dispatch service, lifecycle-aware recovery, parent/child atomic gate, session-board prune
- **Security hardening** — 25+ fixes across HQ auth, ACP WebSocket, CSRF/rebinding, OAuth callback, MCP payload validation, design.materialize containment, YOLO escalation, provider key isolation
- **WebUI shell decomposition** — App.tsx split from 981 to 447 lines (WorkbenchTopbar, PanelSuspense, ViewRouter extracted)
- **TUI rendering** — Link highlighting in markdown renderer, powerline-rail width budgeting fix, silence-terminal stdout.restore leak fix
- **Core tool taxonomy** — Unified ToolErrorInfo attached to all tool error results
- **WebUI responsive** — Viewport hook with centralized breakpoint contract, inspector overlay stacking
- **SAGE auth** — Auth token race fix (cold-spawn + daemon-restart recovery), CI-safe test entry resolution

## Features

- **kanban**: Canonical task classifier (15 queue buckets) with deterministic enforcement
- **kanban**: Board kind system (project, session_mirror, sdd_mirror, import, archive) with retention policies
- **kanban**: Shared dispatch service — 6 operations (reserve, start, complete, fail, cancel, heartbeat) with lease fencing
- **kanban**: Parent/child atomic gate — parent cannot reach Done until all children completed; child-before-parent dispatch ordering
- **kanban**: Session-board prune operation — archive/delete session mirrors past TTL (daily cron job)
- **kanban**: Lifecycle-aware stale recovery preserving managed lifecycle stages
- **tui**: Link highlighting in markdown renderer — `[text](url)` and bare URLs colored in accent
- **cli**: Subcommand docs parity — 7 missing help entries added; manifest generator script
- **core**: Typed environment-variable parsing helpers (envBool, envInt, envFloat, envString, envEnum)
- **core**: Unified tool error taxonomy — structured ToolErrorInfo attached to error results
- **core**: Director task registry prune — `removeTasksOwnedBy` fixes task-worktree/owner leak on FleetManager path
- **webui**: Display settings tab mirroring TUI display fields
- **webui**: Viewport hook (`useViewport`) — centralized breakpoint contract replacing scattered matchMedia calls

## Bug Fixes

### Security (26 fixes)

- **WS-027**: Require a per-process auth token for mailbox IPC — gate every request/shutdown on the daemon's owner-only `.mailbox-server.json` token; client invalidates on refusal and retries on a bounded budget (cold-spawn + daemon-restart recovery)
- **WS-044**: Store HQ browser tokens as SHA-256 verifiers, not cleartext
- **WS-021/052**: Contain design.materialize writes at all four call sites
- **WS-016**: Stop repo-committed prompts from redefining the agent
- **WS-014/022/037/053**: GitHub Pages job permissions + three unwired controls
- **WS-013**: Decouple provider API keys from baseUrl changes
- **WS-012**: HQ auth open-mode floor — require authentication even in local mode
- **WS-011**: HQ auth capability gate — privilege checks before mutations
- **WS-010**: HQ auth expiry — browser tokens now expire
- **WS-009**: Confine HQ query-param browser tokens to loopback
- **WS-008**: Close the YOLO escalation chain
- **WS-007**: Make HQ secret scrubbing unconditional
- **WS-006**: Authenticate the ACP WebSocket transport
- **WS-005**: Require a token for Origin-less WebUI WS clients
- **WS-004**: Validate MCP add/update payloads, harden Win32 MCP spawn
- **WS-003**: Make loopback WS origin trust port-aware
- **WS-002**: Escape OAuth callback HTML + add callback page headers
- **WS-001**: Add CSRF/rebinding guard to the WebUI HTTP surface
- **WS-038**: Gitignore security-report/ and .mailbox-bridge.lock
- **WS-034**: Derive scrubber anchors from patterns, restore plugin parity
- Vault KEK leak closed + CI supply-chain gaps
- atomicWrite mode ceiling, Windows ACL hardening, 0600 on sensitive appends

### Functional

- **tui**: Fix powerline-rail width budgeting — separator cost mismatch (5→2)
- **tui**: Fix silence-terminal stdout.write restore leak in unsilenceTerminal
- **webui**: Fix 8 vertical scroll containers missing min-h-0 constraint
- **webui**: Inspector overlay stacking — WorkspaceDockInspector offsets when InspectorPanel also open
- **sage**: Fix SAGE config drift — remove `bash` from inject.triggers type
- **sage**: Fix auth token race — invalidate cached token on UnauthorizedSageRequest (cold-spawn + daemon-restart)
- **sage**: Fix test spawn on Windows — use `--import tsx` instead of `npx tsx`
- **core**: Fix mailbox credential leak — strip credential object from invalid-secret verification result
- **webui**: Fall back to default sage threshold for invalid persisted values
- **tools**: Add file-size caps to json and diff tools to prevent OOM

## Refactoring

- **webui**: App.tsx shell decomposition Phase 1 — extract WorkbenchTopbar (293 lines)
- **webui**: App.tsx shell decomposition Phase 2 — extract PanelSuspense + ViewRouter (249 lines)
- **webui-hq**: Consolidate HQ CSS monolith (4,340 lines) into 6 focused modules
- **core**: Wire toolErrorResult into executor catch blocks — replace voided classifyToolError
- **simpleui**: Extract settings/prefs + global shortcuts

## Documentation

- ADR-003 task graph marked complete — all 42 nodes across Waves 0-5 done
- Kanban architecture audit doc updated with Phase 0-4 completion status
- TUI feature inventory — 275 source / 266 test files, gaps documented
- WebUI scroll audit — 161 files, 538 overflow patterns mapped
- Telegram v1 handoff assessment — feature-complete, no remaining work

## Architecture Health

- Architecture health check: **PASS** — 28 workspace packages, 2,757 production sources, 2,312 test files
- Hotspot ratchets updated for Kanban architecture program changes
- Core public API snapshot regenerated
- Zero runtime cycles, 16 reviewed type-only cycles
- ADR-003 all 42 nodes marked done (verification, trust boundaries, cross-surface authority, metadata, hotspot decomposition, Core/Runtime ownership)

## Roadmap Status

- **All 34 roadmap cards resolved** across 5 boards (Core, CLI, TUI, WebUI, Plugin)
- 23 stale cards closed (already-done work verified against ADR-003 task graph)
- 11 cards shipped with code changes
- Assessment docs: TUI feature inventory, WebUI scroll audit, Telegram v1 handoff

---

**Full changelog**: `git log v0.297.2..HEAD --oneline` (60+ commits)
